node_modules/
/data/
*.log
.env
.env.*
.DS_Store

# Working notes and the agent's persistent brain stay local, not in git.
brain/
scratch/

# Cached API dumps land next to the loader; never commit them.
src/data/*.json
